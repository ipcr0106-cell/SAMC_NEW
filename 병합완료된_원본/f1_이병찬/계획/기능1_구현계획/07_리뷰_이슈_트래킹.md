# 07. 리뷰 이슈 트래킹

> 작성일: 2026-04-15
> 담당: 병찬 (기능1)
> 출처:
>  - **Phase 4** code-reviewer 에이전트 (이전 세션, 헌법 기반 리뷰)
>  - **/review 스킬** Pass 1·2 체크리스트 정적 스캔
>  - **Adversarial subagent** (신규 각도, chaos eng + input fuzzing)
>
> 본 문서는 **이슈 트래커 역할**. 실제 수정은 별도 지시로 진행.
> 현재 코드에 적용된 수정과 미적용 이슈를 분리해 기록.

---

## 📊 상태 요약 (2026-04-15 수정 세션 반영)

| 심각도 | 총 발견 | 수정 완료 | 미해결 | 팀 합의 대기 |
|-------|--------|---------|------|-----------|
| Critical | **7** | **5** ⬆️+3 | **0** ⬇️-3 | 2 |
| High | **12** | **6** ⬆️+3 | **4** ⬇️-3 | 2 |
| Medium | **10** | 0 | **10** | 0 |
| Low | 7 | 0 | 7 | 0 |

**Critical 0건 남음** — 데모 시연 + 단독 로컬 배포 가능 상태. 팀 공용 배포는 C2(RLS)·C3(JWT) 합의 필요.

---

## 🟢 2026-04-15 수정 세션 로그

이번 세션에서 병찬 단독 처리한 6건 — 전부 **DB 실제 데이터로 검증 완료**.

| # | 이슈 | 상태 | 검증 |
|---|------|-----|------|
| **C-NEW-1** | 복합원재료 2회 매칭 (Adversarial 오탐) | ✅ **주석 강화** | 중복 매칭 안 일어나는 것 코드 재검증 + 설계 주석 추가 |
| **C-NEW-2** | 첨가물 누락 silent pass | ✅ **FIXED** | INS/CAS 있는데 DB 미등록 → `limit_missing` 경보. `"비등록첨가물X" INS 999` 테스트로 경보 1건 확인 |
| **C-NEW-3** | UUID ValueError → 500 | ✅ **FIXED** | `_parse_uuid` 가드 추가. `"bad-id"` → HTTP 400 `INVALID_ID` 확인 |
| **H-NEW-1** | `synthetic_flavor_warning` 매핑 누락 | ✅ **FIXED** | `Feature1Output.synthetic_flavor_ingredients` 필드 추가. `features.py` 에서 status override. `"합성향료(딸기향)"` → 리스트 수집 확인 |
| **H-NEW-3** | `conversion_factor=0/음수` silent pass | ✅ **FIXED** | `convert_unit` 가드 + `float()` 변환 (Decimal 호환). 0/-1.2 → ValueError, 0.847 → 정상 |
| **H-NEW-5** | `name_to_pct` 중복 이름 덮어쓰기 | ✅ **FIXED** | `setdefault + +=` 합산. 안식향산나트륨 2회 0.03%+0.04% → compound total 1185.8 ppm > 600 → **fail 정확 감지** |

### 부가 수정
- **pgbouncer 호환**: `statement_cache_size=0` 추가 (connection.py, bootstrap_f1_db.py, apply_migrations.py, check_db_connection.py) — Supabase Transaction pooler 호환
- **Decimal → float 변환**: asyncpg 가 PostgreSQL NUMERIC 을 Decimal 로 반환하는 문제 해결 (`unit_converter.py`)

### 검증 증거
- 단위 테스트: **76/76 pass 회귀 없음** (수정 전·후 모두)
- DB E2E 5 케이스: 쌀+대두 / 대마초(forbidden) / 초오(prohibited) / 은행종실(restricted+조건부적합) / INS번호 매칭 — 전부 의도대로 판정
- 추가 엣지 케이스 3 건: 합성향료 수집 / 미등록 첨가물 경보 / 동일 첨가물 합산 — 전부 fail 감지 성공

### 남은 미해결 Critical
**없음** ✅ (C2·C3·C4b 는 팀 합의 영역)

---

---

## 🔴 Critical — 수정 완료 (C1·C4)

### C1. `db_manager.py` 쿼리 바인딩 버그 ✅ FIXED
- **출처**: code-reviewer Phase 4
- **파일**: `backend/routers/db_manager.py:230-245`
- **문제**: `params[:-2] if params[:-2] else []` 연산자 우선순위. `only_mine=true` + COUNT 쿼리에서 placeholder/바인딩 불일치 → asyncpg InterfaceError.
- **적용된 수정**: `filter_params = list(params)` 로 WHERE 바인딩만 분리, COUNT 에 별도 전달.
- **검증**: 문법 OK, 이후 리뷰에서 placeholder 번호 재계산 확인됨.

### C4. `main.py` CORS wildcard 미작동 ✅ FIXED
- **출처**: code-reviewer Phase 4
- **파일**: `backend/main.py:78-84`
- **문제**: `allow_origins=["https://*.vercel.app"]` — Starlette CORSMiddleware 는 literal 매칭이라 wildcard 동작 안 함.
- **적용된 수정**: `allow_origin_regex=r"https://.*\.vercel\.app"` 로 분리.

---

## 🔴 Critical — 처리 완료 상세

### C-NEW-1. 복합원재료 중복 매칭 ✅ RESOLVED (Adversarial 오탐, 주석 강화)
- **출처**: Adversarial subagent
- **파일**: `backend/services/step1_ingredients_check.py:255-292` (`evaluate_compound_ingredients`)
- **문제**: H9 재귀 확장 수정 후 유입된 버그.
  - `run_ingredient_match_chain(ing.sub_ingredients)` 로 1단계 매칭
  - 이어서 `evaluate_compound_ingredients(ing.sub_ingredients)` 재귀
  - 재귀 함수가 다시 **같은 `sub_ingredients` 배열을 대상으로** `run_ingredient_match_chain` 호출 → **2회 매칭·2회 에스컬레이션**
- **재현**: `Ingredient(name="과일혼합", sub_ingredients=[Ingredient(name="딸기")])` 1건
- **영향**: 딸기가 prohibited면 `compound_prohibited` 2회 + `prohibited_detected` 1회 적재. `fail_reasons` 중복, 통계 왜곡. 프론트가 "금지 원료 2건 감지" 로 거짓 표시.
- **수정안**: 재귀 호출 대상을 `ing.sub_ingredients` 가 아니라 **해당 sub 각각의 `.sub_ingredients`** 로 한정.
  ```python
  # 현재
  deeper_results, deeper_escalations = await evaluate_compound_ingredients(
      db, ing.sub_ingredients  # ← 여기가 이미 위에서 run_ingredient_match_chain 돈 것
  )

  # 수정 방향
  for sub in ing.sub_ingredients:
      if sub.sub_ingredients:
          deeper = await evaluate_compound_ingredients(db, [sub])
          ...
  ```
- **우선순위**: 🟥 **Critical** — 합성 제품 판정 오류 직결
- **수정 비용**: 백엔드 10줄 / 테스트 2건

### C-NEW-2. `step3_standards` 첨가물 누락 silent pass ✅ FIXED
- **출처**: Adversarial subagent
- **파일**: `backend/services/step3_standards.py:340-350` (`run_step3`)
- **문제**:
  ```python
  for ing in ingredients:
      row = add_rows_by_name.get(ing.name)
      if not row:
          continue  # 해당 첨가물의 기준치 없음 → 넘어감 (허용 범위로 간주)
      additive_checks.append(_check_additive_single(ing, row, process))
  ```
  DB `f1_additive_limits` 시드 누락된 첨가물은 **percentage 무한대여도 pass**. 시드 데이터 커버리지가 완전하지 않으면 **위험한 첨가물이 "수입가능" 으로 판정**.
- **재현**: `Ingredient(name="아황산나트륨", percentage=99.9)` + DB에 해당 행 없음 → overall_status=`pass`
- **영향**: silent wrong answer — 가장 위험한 유형.
- **수정안**:
  ```python
  if not row:
      escalations.append({
          "module_id": "F1",
          "trigger_type": "limit_missing",
          "reason": f'"{ing.name}" 기준치가 DB에 없음 — 담당자 확인 필요',
      })
      # additive_checks 에 no_data 로 추가하거나
      # overall 을 review_needed 로 강등
      continue
  ```
- **우선순위**: 🟥 **Critical** — 개발계획서 §4-1-2 리스크 3(LLM 임계값 추출 오류) 연장선
- **수정 비용**: 백엔드 15줄 / 테스트 2건

### C-NEW-3. `db_manager` UUID ValueError → 500 ✅ FIXED
- **출처**: Adversarial subagent
- **파일**: `backend/routers/db_manager.py:_check_ownership`, `update_row`, `delete_row`, `mark_verified`
- **문제**: 경로 파라미터 `row_id` 가 UUID 형식이 아닐 때 (`""`, `"0"`, `"abc"`) `UUID(row_id)` → `ValueError` → FastAPI 500 에러. 사용자에게 "내부 오류"로 보이지만 실제는 사용자 입력 오류(400).
- **재현**: `curl -X DELETE /api/v1/admin/db/f1_allowed_ingredients/not-a-uuid`
- **영향**: 사용자가 잘못된 URL 클릭 시 500 에러. 로그 노이즈 + 디버깅 혼란.
- **수정안**: `_check_ownership` 진입 시 UUID 변환 try/except → HTTPException(400, "INVALID_ID").
- **우선순위**: 🟥 **Critical** (UX 심각) 또는 🟧 **High** (기능 영향 없음)
- **수정 비용**: 5줄

---

## 🟧 Critical / High — 팀 합의 필요 (병찬 단독 해결 불가)

### C2. RLS 정책 부재 ⏸ AWAITING TEAM
- **출처**: code-reviewer Phase 4
- **상태**: `backend/db/migrations/009_f1_rls_policies.sql` 초안 작성됨. `ENABLE ROW LEVEL SECURITY` + 정책 4개 (SELECT/INSERT/UPDATE/DELETE) × 5개 테이블.
- **블로커**: Supabase 서비스 롤(DATABASE_URL 직접 접속) 이 RLS 우회하므로 `FORCE ROW LEVEL SECURITY` 여부를 인프라 담당과 합의 필요.
- **수정 비용**: 0 (초안 완성). 팀 리뷰 + 실행.

### C3. JWT 검증 미구현 (X-User-Id 위조 가능) ⏸ AWAITING TEAM
- **출처**: code-reviewer Phase 4
- **현재 상태**: `main.py` 상단에 "데모 전용 인증 한계" 주석으로 명시.
- **블로커**: Supabase JWT 미들웨어(`fastapi-jwt` 등) 도입은 인프라 담당 영역. frontend/services/apiClient.ts 의 `Authorization: Bearer` 와 backend 를 연결해야 함.
- **수정 비용**: 백엔드 50줄 + 인프라 합의

### C4b. `main.py` 전원 합의 PR ⏸ AWAITING TEAM
- **현재 상태**: 파일 상단에 "전원 합의 필수 — 초안(병찬 작성)" 주석 명시.
- **블로커**: 팀컨벤션 §2-4 공유 파일.
- **추천 조치**: 채팅 공지 + PR + 세연·아람·성은·인프라 Reviewer 지정.

---

## 🟧 High — 수정 완료

### H5. `step3_standards` overall 데드코드 ✅ FIXED
- **적용**: 빈 리스트일 때 `review_needed` 오판 해결 + 불가능 분기 제거.

### H6. N+1 쿼리 ✅ FIXED
- **적용**: `_evaluate_compound_groups` 가 `add_rows_by_name` 딕셔너리 재사용.

### H8. `_to_pipeline_result` no_data 처리 ✅ FIXED
- **적용**: `actual_value=None` 허용 + `threshold_text` 추가.

### H9. 복합원재료 재귀 확장 ✅ FIXED → 🟥 C-NEW-1 버그 유입
- **주의**: 재귀 수정 자체는 완료됐지만 **중복 매칭 버그가 새로 생김**. C-NEW-1 처리 필수.

### H10. 학명 ILIKE 길이 조건 ✅ FIXED
- **적용**: `len(name) >= 4` 조건 추가.

---

## 🟧 High — 미해결 (신규 발견)

### H-NEW-1. Enum 불일치: `synthetic_flavor_warning` 매핑 누락 ✅ FIXED
- **출처**: /review 스킬 Pass 1 Enum & Value Completeness
- **파일**: `backend/routers/features.py:91-95` ↔ `frontend/types/pipeline.ts:43`
- **문제**: 팀 약속 타입은 `"allowed" | "not_found" | "synthetic_flavor_warning"` 3값. 백엔드는 2값만 생성.
- **결과**: 프론트가 합성향료 경고와 진짜 미확인을 구분 못함.
- **수정안 A**: `_to_pipeline_result` 에서 escalations 의 `trigger_type=="synthetic_flavor"` reason 정규식 파싱.
- **수정안 B (권장)**: `Feature1Output` 에 `synthetic_flavor_ingredients: list[str]` 필드 추가, step1 에서 채움.
- **수정 비용**: 수정안 B = 백엔드 15줄 + 테스트 1건

### H-NEW-2. `feature1.py __to_forbidden_hit` silent drop 🟥 OPEN
- **출처**: Adversarial subagent
- **파일**: `backend/services/feature1.py:125-133`
- **문제**: `try/except Exception: return None` → ForbiddenHit 스키마 불일치 시 조용히 drop. Step 0 검출됐는데 `forbidden_hits=[]` 로 표시될 수 있음.
- **수정안**: 예외 swallow 제거, raise + 에스컬레이션 기록.
- **수정 비용**: 5줄

### H-NEW-3. `conversion_factor=0` silent pass ✅ FIXED
- **출처**: Adversarial subagent
- **파일**: `backend/utils/unit_converter.py:128`, `step3_standards.py:128`
- **문제**: DB 시드 실수로 `conversion_factor=0` 이면 `actual_ppm=0` → 항상 pass. 보존료가 무조건 안전 판정.
- **수정안**: `convert_unit` 에서 `if factor is not None and factor <= 0: raise ValueError`
- **수정 비용**: 3줄

### H-NEW-4. `actual_value` 파싱 로케일 의존 🟧 OPEN
- **출처**: Adversarial subagent
- **파일**: `backend/routers/features.py:142-156`
- **문제**: `float(c.actual_value.split()[0])` — 천단위 콤마(`"1,000.5 ppm"`) 들어가면 ValueError.
- **수정안**: `LimitCheckResult` 에 `actual_numeric: float | None` + `actual_unit: str` 분리 필드 추가.
- **수정 비용**: 15줄 + 테스트 1건

### H-NEW-5. `name_to_pct` 중복 이름 덮어쓰기 ✅ FIXED
- **출처**: Adversarial subagent
- **파일**: `backend/services/step3_standards.py:179`
- **문제**: `name_to_pct = {i.name: i.percentage for i in ingredients}` 딕셔너리 컴프리헨션이라 **같은 이름이 두 번 오면 마지막만 살아남음**. compound 합산 시 첫 행 무시됨.
- **재현**: `ingredients=[Ingredient("아질산나트륨", 0.005), Ingredient("아질산나트륨", 0.003)]`
- **영향**: 합산 0.008% 여야 하는데 0.003% 만 계산. 기준 미달 안 잡힘.
- **수정안**:
  ```python
  name_to_pct: dict[str, float] = {}
  for i in ingredients:
      if i.percentage is not None:
          name_to_pct[i.name] = name_to_pct.get(i.name, 0.0) + i.percentage
  ```
- **수정 비용**: 5줄

### H7. `pipeline_steps` 스키마 계약 ⏸ AWAITING TEAM
- **출처**: code-reviewer Phase 4
- **블로커**: 공통 인프라 담당 합의 필요.

### H11·H12. `.env` 경로 충돌 / `admin_laws` prefix ⏸ AWAITING TEAM
- **담당**: 성은 (기능4)

---

## 🟨 Medium — 미해결 (선택적)

### M-NEW-1. `POST /run` 동시 실행 race
- **파일**: `features.py:265-289`
- **문제**: 같은 `case_id` 동시 요청 시 LLM API 2회 호출 + DB writes 2배. ON CONFLICT 로 ai_result 는 한쪽으로 굳지만 비용 낭비.
- **수정안**: `pg_advisory_xact_lock(hashtext(case_id))` 또는 idempotency key.

### M-NEW-2. `chunker` 빈 줄 구분 다중 표
- **파일**: `utils/chunker.py:104-113`
- **문제**: 빈 줄로 구분된 다중 표가 별도 청크화 → 후속 각주가 엉뚱한 표에 붙음.
- **수정안**: `in_table` 종료 조건에 빈 줄 1개는 skip.

### M-NEW-3. migrations/002 기존 데이터 위 재실행
- **파일**: `migrations/002_f1_allowed_ingredients.sql:35-37`
- **문제**: 컬럼 추가 후 default `'permitted'` 로 채워지면, 기존 미검증 행이 자동으로 permitted 분류 → forbidden 게이트 우회 위험.
- **수정안**: 컬럼 추가 후 별도 UPDATE 로 검증된 행만 status 부여.

### 기타 Medium (code-reviewer Phase 4 지적, 추후)
- M13. `check_forbidden_first` aliases 매칭 케이스 민감 (trim/lowercase 처리)
- M14. `_record_to_json` JSON 파싱 실패 원문 보존
- M15. `saveEdit` try/finally 누락
- M17. `is_satisfied=None` + `part=None` 시 에스컬레이션 자동 생성
- M18. `updated_at` TZ 일관성

---

## 🟦 Low — 미해결 (보류)

Phase 4 리뷰 L20~L26 (모델명 표기, GMO 상수 미사용, Pydantic bool/enum, 이모지 접근성, 한글 파라미터명 등) 전부 데모 이후 정리.

---

## 🚫 수정하면 안 되는 항목 (다른 팀원 영역)

| 항목 | 담당 | 조치 |
|------|------|------|
| `admin_laws.py` prefix 규칙 위반 | 성은 | 채팅에 전달만 |
| `frontend/services/apiClient.ts` | 공통 인프라 | 수정 금지 |
| `frontend/types/pipeline.ts` | 팀 약속 | 확장 필요 시 전원 합의 |
| `backend/db/combined_schema.sql` | 팀 전체 | 수정 금지 |

---

## 📋 Plan Completion Audit 결과

### 구현계획 §00 마스터_로드맵 기준

| 상태 | 항목 수 | 비고 |
|------|--------|-----|
| ✅ DONE | 20+ | 마이그레이션·시드·서비스·라우터·프론트 핵심 |
| ↻ CHANGED (목표 달성) | 3 | T9 → db_manager / P2 → P1 통합 / lib/api.ts → features/*/api |
| ⚠️ PARTIAL | 1 | 별표1 시드 110건 (목표 200) |
| ❌ NOT DONE | 4 | 통합/E2E 테스트, requirements.txt, black/isort, 실제 DB 실행 |

---

## 📊 Test Coverage Diagram (요약)

```
순수 로직 (DB 불필요):       76/76 ★★★
DB 의존 (통합 테스트):        0/N   GAP
프론트 렌더링 (시각):          0/N   GAP (tsconfig 미완)
```

**COVERAGE WARNING**: DB·E2E 커버리지 0%. `bootstrap_f1_db` 적용 + aiosqlite mock 통합 테스트 추가 권장.

---

## 🛠 권장 다음 스텝 (병찬 관점, 우선순위순)

| # | 작업 | 소요 | 심각도 |
|---|------|------|-------|
| 1 | C-NEW-1 복합원재료 중복 매칭 수정 | 15분 | 🟥 |
| 2 | C-NEW-2 첨가물 누락 silent pass 수정 | 15분 | 🟥 |
| 3 | C-NEW-3 UUID ValueError → 400 변환 | 10분 | 🟥 |
| 4 | H-NEW-3 conversion_factor 0 가드 | 5분 | 🟧 |
| 5 | H-NEW-5 name_to_pct 합산 로직 | 5분 | 🟧 |
| 6 | H-NEW-1 synthetic_flavor_warning 매핑 | 20분 | 🟧 |
| 7 | 추가 단위 테스트 (위 수정 검증) | 30분 | 🟧 |
| 8 | 별표1 시드 90건 확장 (110→200) | 45분 | 🟨 |
| 9 | 통합 테스트 aiosqlite mock | 2시간 | 🟨 |
| 10 | `requirements.txt` PR (팀 합의) | 30분 | 🟨 |
| 11 | C2 RLS 정책 팀 리뷰 | — | ⏸ |
| 12 | C3 JWT 미들웨어 (인프라 담당) | — | ⏸ |

---

## 📝 변경 이력

| 날짜 | 변경 | 작성자 |
|------|------|-------|
| 2026-04-15 | 초기 작성 — Phase 4 + /review + Adversarial subagent 통합 | 병찬 |

---

**본 문서는 지속 업데이트됨.** 이슈가 수정되면 상태를 ✅ FIXED 로 바꾸고 변경 이력 추가.
