# ── Supabase ──
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ── Backend API ──
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# ── 아래 키는 프론트에서 직접 사용하지 않음 (백엔드 .env 참고) ──
# OPENAI, PINECONE 등은 backend/.env.example 참조
